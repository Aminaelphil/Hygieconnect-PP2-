{% extends 'base.html.twig' %}

{% block title %}Nos Formations{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/formation.css') }}">
{% endblock %}

{% block body %}
<style>
.hero {
  background: linear-gradient(rgba(0,0,0,0.55), rgba(0,0,0,0.35)), 
              url('{{ asset('images/back2.jpg') }}') center/cover fixed;
  color: white;
  text-align: center;
  padding: 100px 20px;
  position: relative;
  overflow: hidden;
}
</style>

<!-- HERO -->
<section class="hero">
  <h1>Formations Professionnelles</h1>
  <p>Développez vos compétences grâce à nos programmes conçus par des experts du terrain.</p>
  <a href="{{ path('app_demande_new') }}" class="btn-gradient">Demander une formation</a>
</section>

<!-- EXPLICATION -->
<section class="container py-5">
  <h2 class="section-title">Qu’est-ce qu’une formation ?</h2>
  <div class="explanation">
    Une formation est un <strong>processus d’apprentissage</strong> alliant théorie et pratique.
    Elle vise à <strong>développer les compétences</strong> et à <strong>acquérir les connaissances</strong>
    nécessaires pour renforcer les pratiques professionnelles sur le terrain.
  </div>
</section>

<!-- FORMATIONS DYNAMIQUES -->
<section class="container pb-4">
  <div class="row g-4">
    {% if formations is empty %}
      <p class="text-center text-muted">Aucune formation disponible pour le moment.</p>
    {% else %}
      {% for formation in formations %}
        {# Option : Si une formation a un titre spécifique, on peut la rendre plus large #}
        {% set isWide = formation.titre matches '/Hygiène de base/' %}

        <div class="col-md-6 {{ isWide ? 'col-lg-12' : 'col-lg-4' }} animate-card">
          <div class="card modern-card h-100 {{ isWide ? 'card-wide' : '' }}">
            <div class="position-relative">
              <img src="{{ asset('images/' ~ (formation.image ?? 'formation1.jpg')) }}" 
                   class="card-img-top" alt="{{ formation.titre }}">
              <div class="badge-container">
                <span class="badge badge-gradient">{{ formation.duree ?? 'Durée non précisée' }}</span>
                <span class="badge badge-price">{{ formation.prix }} DT/jour</span>
              </div>
            </div>

            <div class="card-body">
              <h5 class="card-title mt-3">{{ formation.titre }}</h5>
              <p class="card-description mt-2">{{ formation.description }}</p>
            </div>
          </div>
        </div>
      {% endfor %}
    {% endif %}
  </div>
</section>

<!-- FOOTER CTA -->
<section class="footer-cta">
  <h3>Besoin d'une formation sur mesure ?</h3>
  <p>Nos experts conçoivent des programmes adaptés à vos besoins et à votre environnement de travail.</p>
  <a href="{{ path('app_demande_new') }}" class="btn">Contactez-nous</a>
</section>

<script>
const cards = document.querySelectorAll('.animate-card');
function showCardsOnScroll() {
  cards.forEach(card => {
    const rect = card.getBoundingClientRect();
    if (rect.top < window.innerHeight - 100) {
      card.classList.add('visible');
    }
  });
}
window.addEventListener('scroll', showCardsOnScroll);
window.addEventListener('load', showCardsOnScroll);
</script>
{% endblock %}
