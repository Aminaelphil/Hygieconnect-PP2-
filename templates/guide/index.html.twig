{% extends 'base.html.twig' %}

{% block title %}Nos Guides{% endblock %}

{% block body %}
<style>
/* === HERO SECTION === */
.hero {
  background: linear-gradient(rgba(0,0,0,0.55), rgba(0,0,0,0.35)), 
              url('{{ asset('images/formation.webp') }}') center/cover fixed;
  color: white;
  text-align: center;
  padding: 100px 20px;
  position: relative;
  overflow: hidden;
}
.hero h1 {
  font-size: 3rem;
  font-weight: 700;
  text-shadow: 2px 2px 6px rgba(0,0,0,0.6);
}
.hero p {
  font-size: 1.2rem;
  max-width: 700px;
  margin: 20px auto;
  text-shadow: 1px 1px 4px rgba(0,0,0,0.6);
}
.btn-gradient {
  background: linear-gradient(135deg, #0d6efd, #00aaff);
  color: #fff;
  border-radius: 30px;
  padding: 12px 28px;
  font-weight: 600;
  transition: transform 0.3s, box-shadow 0.3s;
  text-decoration: none;
}
.btn-gradient:hover {
  transform: translateY(-3px);
  box-shadow: 0 10px 20px rgba(0,123,255,0.3);
}

/* === TITRE PRINCIPAL === */
h2.section-title {
  position: relative;
  font-family: "Raleway", sans-serif;
  font-weight: 300;
  font-size: 38px;
  color: #080808;
  text-align: center;
  text-transform: uppercase;
  margin-bottom: 60px;
}
h2.section-title:before {
  content: "";
  width: 30px;
  height: 5px;
  position: absolute;
  bottom: -8px;
  left: 50%;
  transform: translateX(-50%);
  background-color: #0d6efd;
  border-radius: 2px;
}
h2.section-title:after {
  content: "";
  width: 110px;
  height: 2px;
  position: absolute;
  bottom: -18px;
  left: 50%;
  transform: translateX(-50%);
  background-color: #0078ff;
  opacity: 0.8;
}

/* === SECTION EXPLICATION === */
.explanation {
  text-align: center;
  font-size: 1.1rem;
  max-width: 850px;
  margin: 0 auto 30px auto; /* réduit l'espace sous le texte */
  color: #333;
  line-height: 1.7;
}

/* === STRUCTURE DES CARTES === */
.modern-card {
  background: #ffffff;
  border: none;
  border-radius: 18px;
  box-shadow: 0 6px 20px rgba(0,0,0,0.08);
  overflow: hidden;
  transition: transform 0.4s ease, box-shadow 0.4s ease;
  position: relative;
}
.modern-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 20px 40px rgba(0,123,255,0.25);
}
.modern-card img {
  height: 220px;
  object-fit: cover;
  border-bottom: 1px solid #eee;
  width: 100%;
  display: block;
  position: relative;
}

/* === BADGES SUR IMAGE === */
.badge-container {
  position: absolute;
  top: 15px;
  left: 15px;
  display: flex;
  flex-direction: column;
  gap: 6px;
}
.badge-gradient {
  background: linear-gradient(135deg, #0d6efd, #00aaff);
  color: #fff;
  font-weight: 600;
  border-radius: 12px;
  padding: 6px 12px;
  font-size: 0.85rem;
  box-shadow: 0 3px 6px rgba(0,0,0,0.15);
}
.badge-price {
  background: linear-gradient(135deg, #0078ff, #00bfff);
  color: #fff;
  font-weight: 600;
  border-radius: 12px;
  padding: 6px 12px;
  font-size: 0.85rem;
  box-shadow: 0 3px 6px rgba(0,0,0,0.15);
}

/* === CONTENU DES CARTES === */
.card-body {
  padding: 20px;
}
.card-title {
  font-family: 'Poppins', sans-serif;
  font-weight: 600;
  font-size: 1.2rem;
  color: #0d6efd;
  margin-top: 10px;
}
.card-description {
  font-size: 0.95rem;
  color: #333;
  margin-top: 8px;
  line-height: 1.5;
}

/* === ANIMATION D'APPARITION === */
.animate-card {
  opacity: 0;
  transform: translateY(25px);
  transition: all 0.6s ease;
}
.animate-card.visible {
  opacity: 1;
  transform: translateY(0);
}

/* === FOOTER CTA === */
.footer-cta {
  background: linear-gradient(135deg, #0062cc, #00c6ff);
  color: white;
  text-align: center;
  padding: 70px 20px;
}
.footer-cta h3 {
  font-weight: 700;
}
.footer-cta p {
  font-size: 1.1rem;
}
.footer-cta .btn {
  background: white;
  color: #0062cc;
  font-weight: 600;
  border-radius: 30px;
  padding: 10px 25px;
  transition: all 0.3s ease;
}
.footer-cta .btn:hover {
  background: #004a99;
  color: white;
  transform: translateY(-3px);
}
</style>

<!-- HERO SECTION -->
<section class="hero">
  <h1>Nos Guides</h1>
  <p>Manuels pratiques pour la gestion des risques et les bonnes pratiques professionnelles.</p>
  <a href="{{ path('app_demande_new') }}" class="btn-gradient">Demander un guide</a>
</section>

<!-- EXPLICATION -->
<section class="container py-5">
  <h2 class="section-title">Qu’est-ce qu’un guide ?</h2>
  <div class="explanation">
    Un guide est un <strong>manual pratique</strong> qui sert d’outil de gestion des risques.  
    Il permet de suivre les procédures, d’appliquer les bonnes pratiques et d’améliorer la sécurité et la conformité de vos installations.
  </div>
</section>

<!-- GUIDES -->
<section class="container pb-4">
  <div class="row g-4">
    {% set guides = [
      {'titre': 'Mise en place d\'un guide des BPTE spécifique aux installations de l\'établissement', 'jours': 6, 'prix': 250, 'image': 'guide2.png', 'description': 'Guide complet pour la mise en place des Bonnes Pratiques de Traitement et d’Entretien adaptées à votre établissement.'},
      {'titre': 'Mise à jour du Guide des Bonnes Pratiques de Traitement et d\'Entretien en cas de travaux ou modification des locaux techniques', 'jours': 3, 'prix': 250, 'image': 'guide2.png', 'description': 'Guide pratique pour actualiser vos procédures lors de travaux de rénovation ou de modifications techniques.'}
    ] %}

    {% for guide in guides %}
      <div class="col-md-6 col-lg-6 animate-card">
        <div class="card modern-card h-100">
          <div class="position-relative">
            <img src="{{ asset('images/' ~ guide.image) }}" class="card-img-top" alt="{{ guide.titre }}">
            <div class="badge-container">
              <span class="badge badge-gradient">{{ guide.jours }} jour{{ guide.jours > 1 ? 's' : '' }}</span>
              <span class="badge badge-price">{{ guide.prix }} DT</span>
            </div>
          </div>
          <div class="card-body">
            <h5 class="card-title mt-3">{{ guide.titre }}</h5>
            <p class="card-description">{{ guide.description }}</p>
          </div>
        </div>
      </div>
    {% endfor %}
  </div>
</section>

<!-- FOOTER CTA -->
<section class="footer-cta">
  <h3>Besoin d’un guide personnalisé ?</h3>
  <p>Nos experts vous accompagnent pour mettre en place des outils pratiques adaptés à votre établissement.</p>
  <a href="{{ path('app_demande_new') }}" class="btn">Contactez-nous</a>
</section>

<script>
const cards = document.querySelectorAll('.animate-card');
function showCardsOnScroll() {
  cards.forEach(card => {
    const rect = card.getBoundingClientRect();
    if (rect.top < window.innerHeight - 100) {
      card.classList.add('visible');
    }
  });
}
window.addEventListener('scroll', showCardsOnScroll);
window.addEventListener('load', showCardsOnScroll);
</script>
{% endblock %}
