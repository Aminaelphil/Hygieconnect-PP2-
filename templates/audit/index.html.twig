{% extends 'base.html.twig' %}

{% block title %}Nos Audits{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/audit.css') }}">
{% endblock %}

{% block body %}
<style>
.hero {
  background: linear-gradient(rgba(0,0,0,0.55), rgba(0,0,0,0.35)), 
              url('{{ asset('images/back5.jpg') }}') center/cover fixed;
  color: white;
  text-align: center;
  padding: 100px 20px;
  position: relative;
  overflow: hidden;
}
</style>

<!-- HERO -->
<section class="hero">
  <h1>Nos Audits</h1>
  <p>Évaluation complète par notre expert pour maîtriser les risques et améliorer vos pratiques.</p>
  <a href="{{ path('app_demande_new') }}" class="btn-gradient">Demander un audit</a>
</section>

<!-- EXPLICATION -->
<section class="container py-5">
  <h2 class="section-title">Qu’est-ce qu’un audit ?</h2>
  <div class="explanation">
    Un audit est une <strong>vérification et évaluation</strong> menée par notre expert.
    Il permet d’identifier les risques, de mesurer la conformité et de définir un <strong>score en pourcentage</strong>
    pour piloter efficacement vos installations et vos pratiques professionnelles.
  </div>
</section>

<!-- AUDITS DYNAMIQUES -->
<section class="container pb-4">
  <div class="row g-4">
    {% if audits is empty %}
      <p class="text-center text-muted">Aucun audit disponible pour le moment.</p>
    {% else %}
      {% for audit in audits %}
        <div class="col-md-6 col-lg-4 animate-card">
          <div class="card modern-card h-100">
            <div class="position-relative">
              <img src="{{ asset('images/' ~ (audit.image ?? 'formation3.jpg')) }}" 
                   class="card-img-top" alt="{{ audit.titre }}">
              <div class="badge-container">
                <span class="badge badge-gradient">{{ audit.duree ?? 'Durée non précisée' }}</span>
                <span class="badge badge-price">{{ audit.prix }} DT</span>
              </div>
            </div>

            <div class="card-body">
              <h5 class="card-title mt-3">{{ audit.titre }}</h5>
              <p class="audit-description">{{ audit.description }}</p>
            </div>
          </div>
        </div>
      {% endfor %}
    {% endif %}
  </div>
</section>

<!-- FOOTER CTA -->
<section class="footer-cta">
  <h3>Besoin d’un audit personnalisé ?</h3>
  <p>Notre expert vous accompagne pour sécuriser vos installations et optimiser vos procédures.</p>
  <a href="{{ path('app_demande_new') }}" class="btn">Contactez-nous</a>
</section>

<script>
const cards = document.querySelectorAll('.animate-card');
function showCardsOnScroll() {
  cards.forEach(card => {
    const rect = card.getBoundingClientRect();
    if (rect.top < window.innerHeight - 100) {
      card.classList.add('visible');
    }
  });
}
window.addEventListener('scroll', showCardsOnScroll);
window.addEventListener('load', showCardsOnScroll);
</script>
{% endblock %}
